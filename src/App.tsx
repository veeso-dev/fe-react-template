import * as React from "react";

import { HashRouter } from "react-router-dom";

import Router from "./Router";

const App = () => {
  return (
    <HashRouter>
      <Router />
    </HashRouter>
  );
};

export default App;
